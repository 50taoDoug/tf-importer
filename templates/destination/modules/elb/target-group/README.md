# ELB Target Group

Manages observed `ip` and `instance` target groups, including health and target
group health behavior. Target groups that forward to another ALB remain native
because their generated configuration requires lifecycle exceptions.
