# Lambda Function

Manages the Lambda function variants observed in the imported DEV and PRD
inventories. Deployment package changes remain lifecycle-managed outside the
import-only modularization flow.
